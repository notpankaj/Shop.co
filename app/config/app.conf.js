const KEYS = require("./keys");

// SERVER DETAILS
const HOSTNAME = KEYS.SERVER_HOSTNAME;
const PORT = KEYS.SERVER_PORT;

const SERVER = {
  hostname: HOSTNAME,
  port: PORT,
};

module.exports = SERVER;
